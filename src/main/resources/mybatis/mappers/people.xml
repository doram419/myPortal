<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="people">
	<select id="getList" resultType="PeopleVo">
		SELECT people_id, people_name, phone_number, local_number FROM people
	</select>
	
	<insert id="insert" parameterType="PeopleVo">
		<selectKey keyProperty="no" resultType="long" order="BEFORE">
			SELECT seq_people_id.nextVal FROM DUAL
		</selectKey>
		
		<![CDATA[
			INSERT INTO people
			VALUES(#{no}, #{name}, #{phoneNumber}, #{localNumber})
		]]>
	</insert>
	
	<select id="searchByName" parameterType="String"
		resultType="PeopleVo">
		SELECT 
			people_id, people_name, 
			phone_number, local_number 
		FROM people
		WHERE people_name LIKE('%#{value}%')
	</select>
	
	<delete id="delete" parameterType="long">
		DELETE FROM people 
		WHERE people_id = #{no}
	</delete>
</mapper>